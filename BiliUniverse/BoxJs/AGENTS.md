## 项目规范

### 规范入口

- 根规范只记录全仓库通用协作、语言和集成边界；package 专属规则必须写入对应 package 的规范文档。
- 修改 `Packages/<PackageName>` 下的文件前，必须先阅读该 package 的 `AGENTS.md` 与 `.github/copilot-instructions.md`。
- package 规范与根规范冲突时，以更具体的 package 规范为准。
- 新增或修订某个 package 的专属边界时，不得继续堆到根规范；应更新该 package 的 `AGENTS.md`、`.github/copilot-instructions.md` 或 `docs/` 下的规范文档。

### Package 规范索引

### 通用协作规范

#### 变更边界
- 新增非必要的辅助属性、辅助函数或中间件前，必须说明必要性并得到明确批准；参考实现中已经存在的同类结构除外。
- 禁止新增仅有一处调用的辅助函数；只有逻辑足够复杂，且抽离能显著提升可读性与可维护性时才允许例外。
- 存在疑问、歧义或多种可行实施方案时，必须先确认方案，不得自行假设并直接修改。
- 重构或移动文件时，不得删除原注释，只能修改或重写；只有原对象或属性值被删除时，才允许删除对应注释。
- 修复注释只允许用于长期约束、风险或非显然行为；一次性、局部且从代码或 diff 即可理解的修复不得写入注释。

#### 注释与文档
- 对外接口与非直观实现必须使用对应语言的标准文档注释；JSDoc 必须包含参数与返回值签名。
- 所有代码注释必须使用中英双语：内容介绍使用双行，先中文后英文；参数与返回值使用单行中文/英文。
- 专业术语可以保留英文或在中文后用括号注明英文。
- 规范性文档使用 `Specification` 命名；历史计划、调研记录或迁移说明必须在标题或文件名中表达其非当前规范性质。

#### 命名约定
- `UUID` 属性通常命名为 `uid`。
- `PersistentIdentifier` 属性通常命名为 `pid`。
- 命名应表达真实语义范围，不保留已经失真的历史名称。
- 类型、协议、类、扩展或命名空间名称已经表达职责语义时，其成员方法不得重复使用该职责名称或同义前缀；应直接按具体事件、数据方向、生命周期或值语义命名。

#### Git 与提交
- 未经明确要求，不主动创建 commit。
- 提交前必须确认 staged 内容只包含目标变更，不混入无关修改。
- 提交信息必须遵守 Conventional Commits 规范。
- 提交文本使用中文；专有名词可以在中文后用括号注明英文。
- 多 package 或多职责变更必须按 package 或职责分段提交。

#### 发布说明与 Changelog
- Changelog 必须以上一个正式发布版为基线，对比当前正式发布候选的最终代码、产物与用户行为，只记录用户可感知的最终净变化。
- 同一版本反复修订或重新发布时，基线仍是上一个版本的正式发布，不得把前一次同版本发布、开发分支中间状态或某次构建产物当作比较基线。
- 当前开发或发布过程中引入后又修复的问题、被替换或撤销的方案、临时兼容、内部重构、依赖更新以及构建、CI、发布流程修复均属于非定稿过程，不得写入 Changelog。
- “修复”条目只有在该问题真实存在于上一个正式发布版，并且当前正式发布版的最终状态对用户形成净变化时才允许记录；不得记录为完成当前版本而修复的自身实现错误。
- 编写或修订 Changelog 前必须核对上一个正式 tag 与当前候选 tag 的最终差异、发布资产及运行时行为，不得根据 commit 标题或开发过程罗列变更。

### JavaScript / TypeScript 规范

#### 技术栈与模块
- JavaScript/TypeScript 代码采用 Node.js ES Modules。
- 必须遵循 `package.json` 中的 `type: "module"`，不得无故回退到 CommonJS 写法。
- 对外公开接口以 `package.json` 的 `exports` 与 `README.md` 中记录的导入方式为准。
- 未经明确要求，不得随意扩大、重命名或破坏公开导出面。
- 变更已公开模块时，必须同步维护 `.mjs`、`.mts`、`.d.ts` 之间的运行时实现、类型定义与导出结构一致性。

#### 代码风格与组织
- JavaScript/TypeScript 代码必须遵循仓库 Biome 配置与既有代码风格。
- 缩进、引号、行尾与 import 组织方式沿用现有约定。
- 对外接口与非直观实现必须提供完整 JSDoc。
- 未经明确要求，不新增与现有导出结构不匹配的目录层级、封装层或中间适配层。

#### 测试与文档
- JavaScript/TypeScript 测试优先沿用 `node --test` 与 `test/*.js` 的现有模式。
- 新增或变更公开行为时，应同步补充或更新对应测试。
- 对外 API、导入方式、导出结构或行为语义发生变化时，应同步更新 `README.md` 中的示例与说明。
